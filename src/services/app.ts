/*import axios from 'axios';

export const api = axios.create({
    baseURL: "http://localhost:8080",  // Adiciona a barra extra aqui!
});
*/


import axios from 'axios';

export const api = axios.create({
    baseURL: "http://localhost:8080", // Atualizando para o subdomínio correto
});

